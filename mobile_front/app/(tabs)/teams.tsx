import { View, Text } from "react-native";

export default function TeamsScreen() {
  return (
    <View className="h-full justify-center bg-secondary/30">
      <Text className="text-primary self-center">Teams</Text>
    </View>
  );
}
